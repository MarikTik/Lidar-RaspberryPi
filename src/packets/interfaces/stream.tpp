#include "stream.h"

namespace lidar::transmission{
     // template <typename Packet>
     // StreamInterface<Packet>::StreamInterface(std::istream &is)
     //      : _is(is)
     // {
     // }

     // template <typename Packet>
     // StreamInterface<Packet>::~StreamInterface()
     // {
     // }

     // template <typename Packet>
     // std::vector<Packet> StreamInterface<Packet>::read(std::size_t amount)
     // {
     //      return std::vector<Packet>();
     // }
}
     